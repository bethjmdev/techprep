// method chaining is calling one method after another in one continous line of code

//charAt(0) tells you what character is at a given index

let userName = "bro";

let letter = userName.charAt(0).toUpperCase();
console.log(letter);
